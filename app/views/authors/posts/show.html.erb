<% provide(:page_title, @post.title) %>
<% provide(:author, 'active') %>

<div>
  <%= link_to 'Edit', edit_authors_post_path(@post), class: 'btn btn-sm  btn-warning' %>
  <%= link_to 'Delete', authors_post_path(@post), class: 'btn btn-sm  btn-danger',  method: :delete, data: { confirm: 'Jesteś pewien?' } %>
</div>

<div class="row">
  <div class="col-md-10  offset-md-1 col-lg-8 offset-lg-2 text-xs-center">
    <h1><%= @post.title %></h1>
    <p><%= @post.published_date %></p>
    <%= @post.body.html_safe %>
  </div>
</div>
